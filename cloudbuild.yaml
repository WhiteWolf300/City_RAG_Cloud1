steps:
  # Step 1: Build the container image
  # - name: 'gcr.io/cloud-builders/docker'
  #   args: ['build', '-t', 'gcr.io/$PROJECT_ID/city-rag-app', '.']
  #
  #   # Step 2: Push the container image to Artifact Registry (or gcr.io)
  #   - name: 'gcr.io/cloud-builders/docker'
  #     args: ['push', 'gcr.io/$PROJECT_ID/city-rag-app']
  #
  #     # Step 3: Deploy the new image to Cloud Run
  #     - name: 'gcr.io/cloud-builders/gcloud'
  #       args:
  #         - 'run'
  #           - 'deploy'
  #             - 'city-rag-app'
  #               - '--image'
  #                 - 'gcr.io/$PROJECT_ID/city-rag-app'
  #                   - '--region'
  #                     - 'us-central1'
  #                       - '--platform'
  #                         - 'managed'
  #                           - '--allow-unauthenticated'
  #                             id: Deploy
